# A missing peer dependency should fail rather than warn. ADR-0007 exists
# because an unnoticed peer conflict between TypeScript and typescript-eslint
# would have silently degraded linting.
strict-peer-dependencies=true

# Keep the dependency graph honest: a package must declare what it imports.
auto-install-peers=false
resolution-mode=highest
