apiVersion: skillbox.dev/v1alpha1
kind: agent

metadata:
  namespace: skillbox
  name: REPLACE-ME
  version: 0.1.0
  description: TODO describe what this agent does.
  tags:
    - TODO

spec:
  entrypoint: agent.md
  files:
    - agent.md
    - README.md
  install:
    target: .skillbox/agents/REPLACE-ME

  # Required. One line, 10 to 200 characters, stating the agent's role.
  role: TODO state the role this agent performs.

  # Tool names the agent expects a host to provide. Skillbox does not supply them.
  tools:
    - TODO

  # Prompts this agent uses. Anything listed here should also appear in
  # dependencies, or it will not be installed.
  # prompts:
  #   - skillbox/some-prompt
  # dependencies:
  #   - resource: skillbox/some-prompt
  #     version: ^0.1.0

  inputs:
    - name: TODO
      type: string
      required: true
      description: TODO describe this input.

  outputs:
    - name: TODO
      type: string
      description: TODO describe this output.

  permissions:
    - model:invoke

  compatibility:
    skillbox: ">=0.1.0"
