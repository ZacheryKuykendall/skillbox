# skillbox/REPLACE-ME

TODO one line describing what this prompt does.

## What it does

TODO a short paragraph. Say what problem it solves and what makes it worth installing rather than writing from scratch.

## Inputs

| Name   | Type   | Required | Description |
| ------ | ------ | -------- | ----------- |
| `TODO` | string | yes      | TODO        |

## Outputs

| Name   | Type   | Description |
| ------ | ------ | ----------- |
| `TODO` | string | TODO        |

## Installation

```powershell
skillbox add skillbox/REPLACE-ME
```

```bash
skillbox add skillbox/REPLACE-ME
```

Installs to `.skillbox/prompts/REPLACE-ME/`.

## Required permissions

| Permission     | Why                                    |
| -------------- | -------------------------------------- |
| `model:invoke` | The prompt is sent to a language model |

TODO remove any permission the resource does not need, and justify each one that remains.

Permissions are declared by the author and are not enforced by Skillbox in v0.1.0 — see the security model (docs/architecture/security-model.md in the Skillbox repository).

## Configuration

TODO, or "None. Behavior is controlled entirely through the inputs."

## Usage

TODO show how to actually use it.

```text
Follow .skillbox/prompts/REPLACE-ME/prompt.md.

TODO: input value
```

### Worked example

TODO show a real input and the output it produces. This is the most useful part of a resource README, because it tells a reader whether the resource does what they want before they install it.

## Notes

TODO anything a user should know: limitations, when not to use it, what makes results worse.
